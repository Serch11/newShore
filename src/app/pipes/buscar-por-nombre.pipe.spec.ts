import { BuscarPorNombrePipe } from './buscar-por-nombre.pipe';

describe('BuscarPorNombrePipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarPorNombrePipe();
    expect(pipe).toBeTruthy();
  });
});
